# CLAUDE.md

本專案的完整代理指南見 **[AGENTS.md](AGENTS.md)**（Codex / Claude 共用）。動工前請連同 `DEVLOG.md`、`ROADMAP.md` 一起讀。

最關鍵三條，先記住：
1. **原文一律逐字查證（zh.wikisource.org / ctext.org），禁憑記憶杜撰**——查不到就不要加。
2. **維持單檔內嵌**：CORPUS 留在 `index.html`，不要抽成外部 `corpus.json`（會壞 `file://`）。
3. **繁體中文、標點全形**；改完跑語法檢查＋瀏覽器驗證；`main` 可直接 commit，但 push／改 repo 設定先確認。
